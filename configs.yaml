seed: 42
device: cuda

data:
  data_dir: /mnt/c/Users/lhe09/projects/msd_brain
  roi_size: [96,96,96]

training:
  batch_size: 1
  lr: 0.0001
  epochs: 500
  patience: 50

model:
  name: swinunetr
  in_channels: 4
  out_channels: 4

paths:
  output_dir: outputs